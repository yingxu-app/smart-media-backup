# AI 与废片筛选安全原则

1. 基础复制、分类和校验不依赖 AI，关闭 AI 后产品仍可完整备份。
2. AI 只提出事件名或分类建议，用户可以修改；请求失败时回退到日期、设备和媒体类型。
3. 原始照片与视频默认只在本机读取。只有用户明确配置第三方服务时才允许联网。
4. “模糊、闭眼、曝光异常、重复、黑帧”等结果统一放入“待确认废片”，绝不自动删除原卡文件。
5. 百度网盘上传只处理备份完成后的副本，并需要用户自行提供授权凭据。
6. 日志不得记录 API Key、Secret Key、访问令牌或完整私密素材内容。
