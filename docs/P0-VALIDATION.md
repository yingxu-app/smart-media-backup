# 影序 P0 验收记录

版本：v1.0.11  
日期：2026-07-24

## 已闭环

- 取消任务时，尚未处理的文件会明确记为 `failed`，不会被误记为完成。
- 取消、部分完成和异常结束都会保留原始 SD 卡，并在界面报告“原始素材安全、可继续备份”。
- 备份报告会记录 `copied / skipped / failed / verified`，历史数据库会在结束时重新统计，避免报告和历史页数字不一致。
- 重复备份会写入清晰的跳过数量；历史报告仍可打开。
- 异常退出路径会尝试写出可打开的 JSON 报告，并将任务标记为 `error`。
- 旧数据库迁移会自动补齐 `copied_files`、`report_path` 等列，原有历史记录保持不变。

## 自动化验证

`tests/` 全量回归：15/15 通过，覆盖重复备份、校验失败重试、源文件消失、网络通知失败、目标盘满、权限不足、无 SD 卡、取消任务、异常报告、历史导入和状态清零。

## macOS 包验收

- DMG：`YINGXU-macOS-v1.0.11.dmg`
- SHA-256：`bf972f37da74ef445ac404aba163d29e16542923a9227874e8a2dac3f466023c`
- 已完成 DMG 挂载检查。
- 已完成 `.app` 启动与 `/api/status` 检查，返回版本 `1.0.11`。
- 已用旧表结构模拟升级，确认旧历史仍保留且新列自动迁移。

## 仍需真实设备确认的边界

实际拔出物理目标盘、实际锁定文件、系统休眠后恢复，以及在 Finder 中将 DMG 拖入 `/Applications` 的交互验收，必须在对应硬件/系统状态下由用户现场复核；自动化环境采用等价的安全模拟，不会伪称已经完成物理拔盘测试。
