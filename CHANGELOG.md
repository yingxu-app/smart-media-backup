# 更新日志

## v1.0.34 — 2026-09-12

在 Windows 真机跑通源码版与已发布 v1.0.33 安装包后发现并修复以下问题：

- 修复清理原卡在 Windows 上变成永久删除：此前非 macOS 分支直接调用 `os.remove`，素材无法找回；现改为经系统回收站移入（`SHFileOperationW` + `FOF_ALLOWUNDO`），回收站不可用时整个清理流程失败退出，原卡素材保持原样，同时清掉搬走素材后留下的空目录；
- 修复重复备份导致待确认废片文件被反复改名：审片步骤会把"上一轮已归档进待确认废片"的文件再移动一次，由于目标名与自身同名，文件被改写成 `xxx_1`、`xxx_1_1`…；现在已位于待确认废片目录的文件直接跳过，不再重复移动；
- 修复 Windows 预览树随备份次数无限膨胀：预览生成此前每次都走 `while out_path.exists()`，第二轮起会额外产出 `xxx_1.jpg`；现在新增 `_Windows预览/.preview-manifest.json` 记录"素材路径 → 预览文件"，重复备份复用同一张预览；
- 修复"开始备份"失败时界面永久停在 0%：`/api/start_backup` 此前对"未检测到 SD 卡""请选择备份目标位置""正在备份中"等失败一律返回 HTTP 200，而工作台脚本完全不读取响应，导致界面切到进度面板后既无进度也无提示；现在分别返回 400/409，前端检查响应并在失败时退回操作面板并提示原因；
- 修复读卡器兼容隐患：Windows 检测此前只认 `DRIVE_REMOVABLE`，不少读卡器把 SD 卡报成固定磁盘（`DRIVE_FIXED`），导致全程显示"未检测到 SD 卡"；现在来源检测额外纳入非系统盘的固定卷，配合 DCIM/PRIVATE 目录特征甄别，用户也能在"检测到的来源"里手动选择；
- 实时进度补齐校验字段：`/api/status` 此前不含 `verified_files`/`failed_files`，校验结果只能在最终报告查看；现在进度面板实时展示"已校验"与"失败"数量；
- 新增 Windows 实机验收脚本 `e2e_win_check.py`（端到端备份 + 增量幂等）与 `smoke_yingxu_win.py`（22 项接口冒烟），自动测试增至 41 项。

## v1.0.33 — 2026-08-21

- 新增“影序 Pocket”手机端 PWA：同一 Wi-Fi 下通过一次性配对码连接桌面端
- 手机端可查看实时备份进度、近期归档，并填写事件、地点、设备与备注
- 桌面工作台可读取并应用 Pocket 的事件和地点，同时不向手机暴露本机文件路径
- 新增局域网访问隔离：手机只能访问 Pocket 页面和授权接口，无法打开桌面工作台
- Pocket 页面支持添加到 iPhone 主屏幕；明确说明不读取、上传或删除手机原始素材
- 新增 Pocket 配对、鉴权、脱敏、信息回传和局域网边界自动测试

## v1.0.32 — 2026-08-10

- 归档记录的备份位置改为“位置类别 → 具体位置”的两级筛选，本机、外接存储与历史测试目录分开显示；
- 归档列表按备份执行日期折叠，减少同日多任务平铺造成的混乱；
- 本机候选包，待替换现有 v1.0.31 安装版并完成启动验收。

## v1.0.31 — 2026-07-29

- 修复多个首页请求同时遍历同一张 FAT/exFAT 存储卡时，系统文件读取可能长时间阻塞的问题；
- 同一时刻只允许一次真实存储卡扫描，后来的请求复用同卡缓存或短暂等待，不再重复读取原卡；
- 工作台能识别“正在扫描”状态并自动重试，不会误报为未插卡；
- 新增并发扫描回归测试，自动测试总数增至 20 项；
- 保留 v1.0.30 及此前全部 DMG 和官网历史页面，不覆盖旧版本。

## v1.0.30 — 2026-07-29

- 修复本地 AI 服务不可用时首页长时间停留在“等待插入 SD 卡”的问题；
- 存储卡扫描改为完全离线即时返回，AI 命名不再阻塞设备识别；
- 首页扫描和来源刷新加入防重入保护，避免轮询叠加重复请求；
- 新增回归测试，保证开启 AI 时扫描接口也不会调用模型；
- 完成公开 DMG 下载、校验、重装、启动与历史数据保留验收。

## v1.0.29 — 2026-07-29

- 增加目标可写性、目标空间、来源中断和多目标部分失败处理；
- 每个备份目标单独记录成功、跳过、失败与校验数量；
- 报告同时提供 JSON、CSV、Markdown，并可从归档详情下载；
- 新增健康检查、旧数据库迁移回归和跨平台 CI；
- 修复 Windows 工作流覆盖旧 Release 的风险，Gitee 同步不再强制推送；
- 统一版本、隐私、安全、贡献、架构、测试与比赛说明。

历史版本保留在 GitHub Releases，不覆盖旧 DMG。
